import React from "react";

const ChatNotSelected = () => {
	return (
		<div className="sm:w-[60%] w-full h-[80vh] bg-black/40 relative text-white flex justify-center items-center font-semibold">
			<h1>Select a chat to start messaging</h1>
		</div>
	);
};

export default ChatNotSelected;
